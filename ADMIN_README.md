# Админ-панель Delever

Полнофункциональная админ-панель для управления контентом сайта Delever.

## Доступ к админ-панели

**URL:** `/admin/login`

**Тестовые данные для входа:**
- Email: `admin@delever.io`
- Пароль: `admin123`

> ⚠️ **Важно:** В продакшене обязательно измените пароль в файле `src/contexts/AuthContext.tsx` или подключите реальную систему аутентификации.

## Возможности

### 1. Панель управления (Dashboard)
- Обзор статистики контента
- Быстрый доступ к основным разделам
- URL: `/admin`

### 2. Управление контентом
- Редактирование текстов и элементов сайта
- Добавление новых элементов контента
- Поиск по контенту
- URL: `/admin/content`

**Как использовать:**
1. Добавьте новый элемент контента с указанием пути (например: `home.hero.title`)
2. Редактируйте значение элемента
3. Используйте путь для получения контента в компонентах через `useContent().getContentByPath()`

### 3. Управление навигацией
- Редактирование ссылок в хедере
- Управление категориями и ссылками в футере
- URL: `/admin/navigation`

### 4. Медиа-менеджер
- Загрузка изображений и документов
- Управление загруженными файлами
- Получение URL для использования в контенте
- URL: `/admin/media`

**Поддерживаемые форматы:**
- Изображения: все форматы (JPEG, PNG, GIF, WebP и т.д.)
- Документы: PDF, DOC, DOCX

### 5. Настройки сайта
- Общие настройки (название, описание)
- Контактная информация
- Ссылки на социальные сети
- URL: `/admin/settings`

## Использование контента в компонентах

### Пример использования динамического контента:

```tsx
import { useContent } from '@/contexts/ContentContext'

function MyComponent() {
  const { getContentByPath } = useContent()
  
  // Получаем контент по пути
  const title = getContentByPath('home.hero.title')?.value || 'Заголовок по умолчанию'
  
  return <h1>{title}</h1>
}
```

## Хранение данных

Все данные хранятся в `localStorage` браузера:
- `delever-content` - контент
- `delever-navigation` - навигация
- `delever-footer` - ссылки футера
- `delever-media` - медиа-файлы
- `delever-settings` - настройки
- `delever-admin-auth` - статус аутентификации

> 💡 **Для продакшена:** Замените localStorage на API-запросы к бэкенду.

## Структура файлов

```
src/
├── contexts/
│   ├── AuthContext.tsx          # Аутентификация
│   └── ContentContext.tsx       # Управление контентом
├── components/admin/
│   ├── AdminLayout.tsx          # Layout админ-панели
│   └── ProtectedRoute.tsx       # Защита роутов
└── pages/admin/
    ├── Login.tsx                 # Страница входа
    ├── Dashboard.tsx             # Главная панель
    ├── ContentEditor.tsx         # Редактор контента
    ├── NavigationEditor.tsx      # Редактор навигации
    ├── MediaManager.tsx          # Медиа-менеджер
    └── Settings.tsx              # Настройки
```

## Безопасность

1. **Измените пароль по умолчанию** перед деплоем
2. **Используйте переменные окружения** для чувствительных данных
3. **Подключите реальную систему аутентификации** (JWT, OAuth и т.д.)
4. **Добавьте валидацию** на стороне сервера
5. **Ограничьте доступ** по IP или другим методам

## Дальнейшее развитие

- [ ] Интеграция с бэкенд API
- [ ] Загрузка файлов на сервер
- [ ] История изменений (версионирование)
- [ ] Роли и права доступа
- [ ] Экспорт/импорт контента
- [ ] Предпросмотр изменений
- [ ] Мультиязычность

## Поддержка

При возникновении проблем проверьте:
1. Консоль браузера на наличие ошибок
2. localStorage на наличие данных
3. Правильность путей к контенту

